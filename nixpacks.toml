[phases.setup]
nixPkgs = []

[phases.install]
cmds = [
  "pip uninstall opencv-python opencv-contrib-python -y || true",
  "rm -rf /opt/venv/lib/python3.12/site-packages/cv2*",
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
